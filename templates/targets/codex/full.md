<!-- Forge Codex Full Template
     Router-first AGENTS.md for Codex.
     Replace all {{VARIABLES}} with your actual values.
     Generated by Forge (https://github.com/eisen0419/forge) -->

# AGENTS.md

Keep this file short and operational. It is a router and guardrail layer, not a project encyclopedia. Put durable details in docs and point Codex there.

## User

- Name: {{USER_NAME}}
- {{PLATFORM}} / {{SHELL}} / {{PACKAGE_MANAGERS}} / {{EDITOR}}

## Preferences

- {{LANGUAGE_PREFERENCE}}
- Do not commit or push unless explicitly asked.
- Commit format: `<type>(scope): <summary>`; verb-first summary, <= 50 chars, no period.
- Use `gh` for GitHub URLs and GitHub state.
- Prefer `rg` / `rg --files` for search.
- Prefer `apply_patch` for manual edits.
- Be direct. State uncertainty instead of presenting guesses as facts.

## Instruction Priority

1. User's explicit request in the current session
2. Repository rules, docs, and conventions
3. This `AGENTS.md`
4. Skill workflow definitions

## Context Pointers

Read only what the task needs:

- Project overview: `README.md`
- Architecture: `docs/architecture.md` if present
- Decisions: `docs/decisions/` or ADRs if present
- Known solutions: `docs/solutions/`
- GSD state: `.planning/`
- CE plans and brainstorms: `docs/plans/`, `docs/brainstorms/`

If a referenced file does not exist, do not invent it. Inspect the repo and continue with the closest available source.

## Decision Framework

Before changing files, ask:

1. Is this a real problem or imagined?
2. Is there a simpler safe path?
3. What could this break?

## Task Routing

| Type | Criteria | Default workflow |
|------|----------|------------------|
| Read-only | Analysis, explanation, research, review | Answer directly with evidence |
| Investigation | Diagnosis before edits | Reproduce or inspect -> identify cause -> propose fix |
| Lightweight | Single-file or clearly local edit | Implement -> targeted verification |
| Medium | 3+ steps or local behavior change | Short plan -> implement -> verify |
| Large / Risky | Public API, schema, persistence, auth, payments, CI, deps, broad refactor | Plan -> staged implementation -> review -> completion verification |

Ask at most one key question for lightweight work. If a safe assumption is available, state it and proceed.

## Multi-Agent Router

Use Forge as the discipline layer, then route to the strongest installed workflow.

| Situation | Preferred route |
|-----------|-----------------|
| Small fix | This file directly, CE `ce-work`, or GSD `gsd-fast` |
| CE-native feature | `ce-strategy` if needed -> `ce-brainstorm` -> `ce-plan` -> `ce-work` -> `ce-code-review` -> `ce-compound` |
| GSD-managed project | `gsd-map-codebase` -> `gsd-new-project` -> `gsd-discuss-phase` -> `gsd-plan-phase` -> `gsd-execute-phase` -> `gsd-verify-work` -> `gsd-ship` |
| CE plan into GSD | Pass the CE plan into GSD discuss/plan/execute; on Claude Code this may be `/forge-run <plan>` |
| Product, UI, DX, browser QA, release risk | gstack `office-hours` / `autoplan` before implementation; `qa`, `design-review`, `devex-review`, or `ship` before release |

Avoid double ceremony. Do not run CE planning, GSD initialization, gstack autoplan, and local planning for every task.

## Specialized Flow Priority

Debug / TDD / completion verification / code review should prefer Forge + CE / GSD / gstack. Superpowers are fallback discipline only.

| Scenario | Preferred entry | Superpowers fallback |
|----------|-----------------|----------------------|
| Debug | UI/browser: gstack `qa` or `investigate`; GSD: `gsd-debug`; general bug: CE `ce-debug`; small bug: Forge investigation | `systematic-debugging` |
| TDD / test design | gstack `plan-eng-review`; CE `ce-plan` / `ce-work`; GSD `plan-phase` / `execute-phase` | `test-driven-development` for high-risk shared logic, state machines, auth, payments, migrations |
| Completion verification | Forge Verification Rules + Change Delivery Gate; GSD `gsd-verify-work`; gstack `qa`; CE/GSD review | `verification-before-completion` as a reminder, not a default workflow |
| Code review | CE `ce-code-review`; gstack `review`; GSD `gsd-code-review`; gstack `cso` for security | Superpowers review only if requested or no stronger tool is available |

## Do Not Introduce

Do not add or switch these without explicit approval:

- New runtime dependencies or package managers
- New frameworks, test runners, linters, formatters, or build tools
- Database/schema/migration changes
- CI, deployment, or environment template changes
- New global state, background services, or long-running processes
- Secrets, credentials, API keys, or generated private data

## Verification Rules

- Never invent commands, exit codes, test results, or external state.
- Before claiming done, run the smallest relevant verification and read the output.
- If verification cannot run, say why and lower the completion claim.
- For docs/templates, check placeholders, links/paths where practical, and any JSON/TOML/plugin manifests touched.

## Blast Radius

Before touching exported functions, public APIs, shared types, schemas, auth, payments, persistence, CI, or root config:

1. Use `rg` to find callers and dependents.
2. Add impacted modules to the verification checklist.
3. Run verification for the impacted surface after changes.

## Local Instruction Files

Use local `AGENTS.md` files for high-risk subtrees such as `auth/`, `billing/`, `payments/`, `infra/`, `migrations/`, or generated SDKs. Local files should be short and specific:

- What is sensitive here
- What must not change without approval
- Required verification commands
- Pointers to deeper docs

More deeply nested `AGENTS.md` files override broader ones for their subtree.

## Hooks And Memory

Hooks are for objective, low-ambiguity checks only. Good candidates:

- Warn when dependencies or lockfiles change.
- Require verification notes when schema, migrations, auth, payments, or CI change.
- Suggest browser QA when frontend routes/components change.

Do not encode subjective design judgment in hooks.

Long-term memory should live in project artifacts, not in this file:

- Reusable fixes: `docs/solutions/`
- Architecture decisions: `docs/decisions/`
- GSD project state: `.planning/`
- CE learning: `ce-compound`

## Git And Safety

- Branch prefix: `{{BRANCH_PREFIX}}`
- Types: `feat` / `fix` / `docs` / `refactor` / `test` / `chore`
- Never force-push, rewrite pushed history, use `--no-verify`, or run destructive commands unless explicitly requested.
- Do not manipulate `.git` except through git commands.
- Do not kill processes not started by the current task unless asked.

## Roles

| Role | Provider |
|------|----------|
| designer | {{ROLE_DESIGNER}} |
| executor | {{ROLE_EXECUTOR}} |
| inspiration | {{ROLE_INSPIRATION}} |
| reviewer | {{ROLE_REVIEWER}} |

Default test level: {{DEFAULT_TEST_LEVEL}}
