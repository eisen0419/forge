# Claude Code Target

Claude Code is Forge's original target.

Use the top-level templates for this target:

- `templates/essential.md` -> `CLAUDE.md`
- `templates/full.md` -> `CLAUDE.md`

They stay at the top level so existing README links and manual setup flows continue to work.
